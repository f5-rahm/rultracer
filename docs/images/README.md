# Screenshots

Drop the README screenshots here. The top-level `README.md` references these
filenames:

- `rultracer_sequence.png` — Analysis → the TMM ↔ TCL VM sequence diagram / step-through
- `rultracer_flamegraph.png` — Analysis → Flamegraph (aggregated or diff)
- `rultracer_cpu_cycles.png` — Analysis → Stats (cycles/CPU) or an exported report

PNG or WebP is fine; keep them reasonably sized (≈1600px wide max). If you rename
a file, update the matching `![…](docs/images/…)` link in `README.md`.
